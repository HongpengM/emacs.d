(require 'use-package)

;; (use-package dash-at-point
;;   :ensure t
;;   :demand t
;;   :bind (("C-c d" 'dash-at-point)
;; 	 ("C-c e" 'dash-at-point-docsets))
;;   :config
;;   (progn
;;     (add-hook 'prog-mode 'dash-at-point)))

